$$
rx_i = \lor_j rx_i^{(j)} \\
rz_i = \lor_j rz_i^{(j)} \\
s = rx \lor rz \\
w_{tot} = ||s|| \\
cost(B) = w_{tot} * n_{n.l.}^2 + \sum_{i,j} (rx_i \lor rz_i \lor rz_j \lor rz_j) + \frac{1}{2}  (\sum_{i,j}(rx_i\lor rx_j) + (rz_i \lor rz_{j}))
$$

这里取的i,j是column，因为row的话一定单增>0。

可以发现这里cost最少也是0（所有项都只会增加不会减少），可以继续简化为
$$
\mathrm{cost}(B) = \sum_{i,j}(rx_i \lor rx_j) + (rz_i \lor rz_{j})
$$
就足够表示所有信息。（但改成这个以后好像编译结果也有一点变化，我不太清楚为什么）

动态分组过程为在目前的Pauli集合$\{P\}$中选择$cost$最大的，然后依次选择cost最小的（=0）加入当前集合，直到代价>0。

可以发现这个过程其实只有第一个被选择的Pauli会影响分组结果（？（决定了覆盖的rx和rz，如果有新的rx的话一定会有增量？）

继续简化可以变成选择目前cost最大的Pauli String $P$，直接选择它覆盖的子集（改成这个以后结果又有点变化，我也有点搞不清楚为什么x
